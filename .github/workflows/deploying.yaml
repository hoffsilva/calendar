name: Deploying
on:
  # push:
  pull_request:
    branches:
      - development
jobs:
  deploy:
    name: Deploying to Testflight
    runs-on: macOS-latest
    steps:
      - name: Deploying timetable to appstore
      run: bundle exec fastlane timetable_appstore